---
date: 2026-03-29
---

# 2026-03-29

## PWA Safe Area Fix
- Added `env(safe-area-inset-top)` padding to sticky header — fixes iOS status bar overlap in standalone mode
- Bottom nav already had `env(safe-area-inset-bottom)` — now both edges are covered

## Project Codex
- Created `docs/codex/` with README index linking two development tracks
- **App Improvements** (`docs/codex/app-improvements/README.md`): full record of all completed work (12 sections), upcoming priorities, links to plan docs
- **Woodland Grove** (`docs/codex/woodland-grove/`): vision, creatures, art pipeline, player character, technical reference

## Week Strip Date Picker
- New `WeekStrip` component: horizontal 7-day row with Framer Motion slide animation, logged-date dots, today highlight
- `UserPrefsProvider`: context for `weekStartsOn` preference (0=Sunday, 1=Monday)
- Supabase migration: `profiles.week_start` column (default Monday)
- Profile page: "Start week on Sunday" toggle with optimistic update
- Log page: week strip replaces old date bar, full calendar still expandable below

## Unified DatePicker (replaced WeekStrip + Calendar)
- Custom `DatePicker` component replacing both `WeekStrip` and react-day-picker `Calendar`
- Active week row is always visible; expanding reveals rest of month above/below with height animation
- Same `DayCell` subcomponent in both modes — visual consistency by construction
- Deleted `week-strip.tsx`, removed `.day-logged` CSS pseudo-element hack
- Calendar math built with date-fns (no react-day-picker dependency on log page)

## Dynamic Page Header
- `PageHeader` client component reads `usePathname()` → maps to route title + icon
- Replaces static "Weight Tracker" text — now shows "Log", "History", "Dashboard", etc.
- Header hidden on mobile (`hidden sm:block`) — bottom nav is sufficient
- Safe area padding moved to `<main>` via `max(env(safe-area-inset-top), 1.5rem)`

## Outfit Font
- Added Outfit as default `font-sans` via `next/font/google`
- Self-hosted (no external Google Fonts requests), CSS variable `--font-outfit`
- Tailwind config updated: `fontFamily.sans` uses the variable

## Font Hierarchy Polish
- Date label promoted to `text-lg font-bold` (hero context)
- Calories value promoted to `text-lg font-semibold` (key derived insight)
- Macro labels (Carbs/Protein/Fat) demoted to `text-xs` (icons + color suffice)
- "Snap macros" button demoted to `text-xs`
- "Daily Log" card title → subtle uppercase label (`text-xs uppercase muted`)
- Form spacing tightened (`space-y-5` → `space-y-4`)

## Weight Stepper Sizing
- ±0.1 buttons: `h-12 w-12` → `h-14 w-14` (56px touch targets)
- ±1 buttons: `h-11 w-11` → `h-12 w-12`, text `text-xs` → `text-sm`
- Chevron icons: `size-5` → `size-6`
- Row gap: `gap-2` → `gap-3`, added `py-2` breathing room
- More spacing between "Weight (lbs)" label and stepper controls

## Weight Display Cleanup
- `formatWeight()` now drops trailing `.0` — `112.0` displays as `112`, `112.3` stays `112.3`
- Applies to main display, delta badge, and previous-day reference

## Nav Cleanup
- Removed Grove tab from bottom nav, desktop nav, and page header route map
- Grove page still accessible by URL, just no nav entry

## Key decisions
- Built custom month grid instead of using react-day-picker — needed row-level animation control
- Header hidden on mobile since bottom nav provides navigation — reclaims ~56px vertical space
- `UserPrefsProvider` is lightweight context (just `weekStartsOn`) rather than overloading existing providers
