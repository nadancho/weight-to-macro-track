# 2026-03-27

## Weight stepper component
- New `src/components/weight-stepper.tsx` replacing standard number input
- Auto-fills from most recent weight entry (14-day lookback, not just previous day)
- Arrow buttons for ±0.1 (large, primary), text buttons for ±1 (small, secondary)
- Tap center number to fall back to keyboard input
- Delta badge shows change from previous entry (green for loss, red for gain)
- Previous entry label shows "Yesterday" or date (e.g. "Mar 25") depending on recency
- Changed `weight` state from `string` to `number | null` throughout page.tsx

## Macro screenshot button
- Redesigned from dashed border/muted text to solid `bg-secondary` button
- 44px min touch target, larger camera icon (size-5)
- Copy changed to "Snap macros from photo"

## Bottom tab navigation
- New `src/components/bottom-nav.tsx` — fixed bottom bar with 4 tabs
- Glass blur effect matching header (`bg-background/60 backdrop-blur-xl`)
- Active tab highlighting via `usePathname()`
- Safe area padding for notched devices (`env(safe-area-inset-bottom)`)
- Hidden on desktop (`sm:hidden`), top nav links hidden on mobile
- Raccoon animation repositioned above bottom nav (bottom: 88px)

## Cleanup
- Removed `.sfdx/` directory (leftover from uninstalled VS Code extension)
- Added `.sfdx` to `.gitignore`

## Roadmap
Brainstormed phased mobile UX improvement plan:
- **Phase 1** (done): Bottom tabs, weight stepper, safe areas, touch targets
- **Phase 2**: Woodland color palette (earthy greens, browns, warm tones)
- **Phase 3**: Micro-interactions (page transitions, skeleton loaders, button feedback)
- **Phase 4**: Full woodland personality (illustrated elements, raccoon character states)

Next up: Framer Motion page transitions, skeleton loaders, button press feedback
